{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["NEXT16_RECEIVER_URL", "NEXT14_SENDER_URL"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}